version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: brain-tasks-app
        LoadBalancerInfo:
          ContainerName: brain-tasks-app
          ContainerPort: 80
